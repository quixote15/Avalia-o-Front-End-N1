

<div id="{{idModal}}" class="modal">
    <div class="modal-content">
      
        <img class="modal-icon" src="{{rootURL}}/icons/success.svg" alt="sucess icon">
     
        <h1 class="blue-text modal-text">
                PRODUTO ADICIONADO AO CARRINHO
        </h1>
        
    </div>

    <div class="modal-footer">
        <button class="btn btn--dark" {{action (mut open)}}>OK!</button>
    </div>
</div>


{{yield}}